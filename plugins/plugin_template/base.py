def init_plugin(main_app):
    """
    Initialize the plugin and register its features with RepoRocket.
    """
    # Example: show a status message indicating the plugin is loaded.
    main_app.statusBar().showMessage("Plugin 'plugin_template' loaded", 3000)
